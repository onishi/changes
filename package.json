{
  "name": "changes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && wrangler types worker-configuration.d.ts --config dist/changes/wrangler.json && tsc -b",
    "build:production": "CLOUDFLARE_ENV=production vite build && wrangler types worker-configuration.d.ts --config dist/changes/wrangler.json && tsc -b",
    "preview": "vite preview",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build",
    "deploy": "npm run build:production && wrangler deploy",
    "deploy:first": "npm run build:production && wrangler deploy --secrets-file .prod.vars",
    "deploy:dry-run": "npm run build:production && wrangler deploy --dry-run",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "cf-types": "vite build && wrangler types worker-configuration.d.ts --config dist/changes/wrangler.json",
    "d1:migrate:local": "wrangler d1 migrations apply changes-production --local",
    "d1:migrate:production": "wrangler d1 migrations apply changes-production --remote --env production"
  },
  "dependencies": {
    "hono": "^4.13.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.52.1",
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "@cloudflare/workers-types": "^5.20260817.1",
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "eslint": "^10.8.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
